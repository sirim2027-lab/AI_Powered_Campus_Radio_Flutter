import 'package:flutter/material.dart';
class StudentVoiceSubmitScreen extends StatefulWidget{const StudentVoiceSubmitScreen({super.key});@override State<StudentVoiceSubmitScreen> createState()=>_StudentVoiceSubmitScreenState();}class _StudentVoiceSubmitScreenState extends State<StudentVoiceSubmitScreen>{final _form=GlobalKey<FormState>();final _topic=TextEditingController(),_description=TextEditingController();var _category='';static const categories=['Campus Life','Student Achievement','Clubs','Events','Ideas','General'];@override void dispose(){_topic.dispose();_description.dispose();super.dispose();}@override Widget build(BuildContext context)=>Scaffold(appBar:AppBar(backgroundColor:const Color(0xFF312E81),foregroundColor:Colors.white,title:const Text('Submit Voice')),body:Form(key:_form,child:ListView(padding:const EdgeInsets.all(16),children:[const Text('Complete your submission details',style:TextStyle(color:Color(0xFF6B7280))),const SizedBox(height:20),const Text('CATEGORY *',style:TextStyle(fontSize:12,fontWeight:FontWeight.w800,color:Color(0xFF6B7280))),const SizedBox(height:8),Wrap(spacing:8,runSpacing:8,children:categories.map((x)=>ChoiceChip(label:Text(x),selected:_category==x,onSelected:(_)=>setState(()=>_category=x),selectedColor:const Color(0xFF4F46E5),labelStyle:TextStyle(color:_category==x?Colors.white:null),side:BorderSide.none)).toList()),const SizedBox(height:18),TextFormField(controller:_topic,decoration:const InputDecoration(labelText:'Topic *',hintText:'What would you like to share?'),validator:(v)=>v==null||v.trim().isEmpty?'Enter a topic':null),const SizedBox(height:16),TextFormField(controller:_description,maxLines:5,maxLength:500,decoration:const InputDecoration(labelText:'Description *',hintText:'Tell listeners what your segment is about…'),validator:(v)=>v==null||v.trim().isEmpty?'Enter a description':null),const SizedBox(height:10),Container(padding:const EdgeInsets.all(14),decoration:BoxDecoration(color:const Color(0xFFFEF3C7),borderRadius:BorderRadius.circular(14)),child:const Text('Audio upload and Student Voice submission storage are not configured in the current Firebase project. Your details cannot be submitted yet.',style:TextStyle(color:Color(0xFF92400E),fontSize:12,height:1.5))),const SizedBox(height:20),FilledButton(onPressed:(){if(!_form.currentState!.validate()||_category.isEmpty){ScaffoldMessenger.of(context).showSnackBar(const SnackBar(content:Text('Select a category and complete all required fields.')));return;}ScaffoldMessenger.of(context).showSnackBar(const SnackBar(content:Text('Submission is unavailable until Student Voice storage is configured.')));},child:const Text('Submit for Review'))])));}
