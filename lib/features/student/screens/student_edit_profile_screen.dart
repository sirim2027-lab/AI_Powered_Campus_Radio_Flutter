import 'package:flutter/material.dart';import '../../../models/app_user.dart';
class StudentEditProfileScreen extends StatefulWidget{const StudentEditProfileScreen({super.key,required this.user});final AppUser user;@override State<StudentEditProfileScreen> createState()=>_StudentEditProfileScreenState();}class _StudentEditProfileScreenState extends State<StudentEditProfileScreen>{late final TextEditingController _phone,_address;@override void initState(){super.initState();_phone=TextEditingController();_address=TextEditingController();}@override void dispose(){_phone.dispose();_address.dispose();super.dispose();}@override Widget build(BuildContext context)=>Scaffold(appBar:AppBar(backgroundColor:const Color(0xFF312E81),foregroundColor:Colors.white,title:const Text('Edit Profile')),body:ListView(padding:const EdgeInsets.all(16),children:[Center(child:CircleAvatar(radius:44,backgroundColor:const Color(0xFF4F46E5),child:Text(widget.user.name.isEmpty?'S':widget.user.name[0].toUpperCase(),style:const TextStyle(color:Colors.white,fontSize:32)))),const SizedBox(height:24),Container(padding:const EdgeInsets.all(16),decoration:BoxDecoration(color:const Color(0xFFF9FAFB),borderRadius:BorderRadius.circular(18)),child:Column(crossAxisAlignment:CrossAxisAlignment.start,children:[const Text('ACADEMIC INFORMATION (READ-ONLY)',style:TextStyle(fontSize:11,fontWeight:FontWeight.w800,color:Color(0xFF6B7280))),const SizedBox(height:10),_row('Full Name',widget.user.name),_row('Email',widget.user.email),_row('Department',widget.user.department??'Not specified'),_row('Semester',widget.user.semester??'Not specified'),_row('USN',widget.user.studentId??'Not specified'),const Padding(padding:EdgeInsets.only(top:8),child:Text('Contact an administrator to update academic details.',style:TextStyle(fontSize:11,color:Color(0xFF6B7280)))])),const SizedBox(height:20),TextField(controller:_phone,decoration:const InputDecoration(labelText:'Phone Number',hintText:'Profile contact details are not configured')),const SizedBox(height:16),TextField(controller:_address,maxLines:3,decoration:const InputDecoration(labelText:'Residential Address',hintText:'Profile address is not configured')),const SizedBox(height:16),const Text('Contact fields are shown for the reference UI only. Student updates to users/{uid} are not permitted by the current Firestore rules.',style:TextStyle(fontSize:12,color:Color(0xFF6B7280),height:1.5)),const SizedBox(height:20),FilledButton(onPressed:()=>ScaffoldMessenger.of(context).showSnackBar(const SnackBar(content:Text('Profile editing is not available until student profile write access is configured.'))),child:const Text('Save Changes'))]));Widget _row(String label,String value)=>Padding(padding:const EdgeInsets.symmetric(vertical:6),child:Row(children:[Text(label,style:const TextStyle(color:Color(0xFF6B7280))),const Spacer(),Flexible(child:Text(value,textAlign:TextAlign.end,style:const TextStyle(fontWeight:FontWeight.w700)))]));}
